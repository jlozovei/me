const fallback = {
  text: 'sans-serif',
  code: 'monospace'
};

const fonts = {
  title: `Josefin Sans, ${fallback.text}`,
  text: `Inter, ${fallback.text}`,
  code: `Source Code Pro, ${fallback.code}`
};

export { fonts };
