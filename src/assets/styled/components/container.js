import styled from 'styled-components';

import { sizes } from 'assets/styled';

const Container = styled.div`
  position: relative;
  width: 90%;
  max-width: ${sizes.grid_width};
  margin: 0 auto;
`;

export { Container };
